# Mooning Monkey fonts

These match the original site at https://moon-monkey.netlify.app/

| File | Role |
|------|------|
| `Nurom-Bold.ttf` | Headings, hero titles, section titles (`font-display`) |
| `Sansation-Regular.ttf` | Body copy, paragraphs, UI text (`font-body`) |

Sourced from the original Netlify build (`/static/media/`).
